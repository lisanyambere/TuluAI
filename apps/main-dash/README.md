# Tulu facility dashboard

Reserved workspace for the authenticated facility-side application.

The dashboard will let authorized staff review incoming requests, confirm current facility information, manage availability, and coordinate human follow-up. It should consume the agent API rather than contain agent credentials or privileged business logic in the browser.

When implemented, `apps/main-dash` can be configured as its own hosting project and deployment root.
