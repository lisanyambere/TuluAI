# Tulu shared contracts

Reserved workspace for framework-neutral schemas, TypeScript types, event names, and case-status definitions shared by the caller, facility dashboard, and agent API.

Do not place secrets, server-only code, or browser-specific components in this package.
