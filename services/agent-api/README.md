# Tulu agent API

Reserved workspace for server-side voice-agent orchestration, tool execution, authentication, and database access.

OpenAI and infrastructure credentials belong here or in the hosting platform's server-side secret store. They must never be exposed to either frontend application.
